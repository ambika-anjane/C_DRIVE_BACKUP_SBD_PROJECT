create or replace TABLE HTAS_BRAND_SHARE_LANDING (
	METRIC VARCHAR(16777216) NOT NULL COMMENT 'Market metric measured by Numerator',
	PRODUCT_AND_CATEGORY VARCHAR(16777216) NOT NULL COMMENT 'Product parameter for data pull',
	RETAILER VARCHAR(16777216) NOT NULL COMMENT 'Customer group parameter split for data pull',
	BRAND VARCHAR(16777216) NOT NULL COMMENT 'Product brand',
	TIME_SCALE VARCHAR(16777216) NOT NULL COMMENT 'Scale of time frame (e.g. 1 Month Ending, Quarter Ending)',
	TIME_PERIOD VARCHAR(16777216) NOT NULL COMMENT 'Last date to denote end of time_scale',
	TIME_COVERAGE VARCHAR(16777216) NOT NULL COMMENT 'Coverage of time period (e.g. Rolling)',
	VALUE VARCHAR(16777216) COMMENT 'Metric value per the associated parameters',
	LOADDTS VARCHAR(16777216),
	EVENTDTS VARCHAR(16777216),
	REC_SRC VARCHAR(16777216),
	ROW_SQN VARCHAR(16777216),
	primary key (METRIC, PRODUCT_AND_CATEGORY, RETAILER, BRAND, TIME_SCALE, TIME_PERIOD, TIME_COVERAGE)
);


create or replace TABLE OUTDOOR_BRAND_SHARE_LANDING (
	METRIC VARCHAR(16777216) NOT NULL COMMENT 'Market metric measured by Numerator',
	PRODUCT_AND_CATEGORY VARCHAR(16777216) NOT NULL COMMENT 'Product parameter for data pull',
	RETAILER VARCHAR(16777216) NOT NULL COMMENT 'Customer group parameter split for data pull',
	BRAND VARCHAR(16777216) NOT NULL COMMENT 'Product brand',
	TIME_SCALE VARCHAR(16777216) NOT NULL COMMENT 'Scale of time frame (e.g. 1 Month Ending, Quarter Ending)',
	TIME_PERIOD VARCHAR(16777216) NOT NULL COMMENT 'Last date to denote end of time_scale',
	TIME_COVERAGE VARCHAR(16777216) NOT NULL COMMENT 'Coverage of time period (e.g. Rolling)',
	VALUE VARCHAR(16777216) COMMENT 'Metric value per the associated parameters',
	LOADDTS VARCHAR(16777216),
	EVENTDTS VARCHAR(16777216),
	REC_SRC VARCHAR(16777216),
	ROW_SQN VARCHAR(16777216),
	primary key (METRIC, PRODUCT_AND_CATEGORY, RETAILER, BRAND, TIME_SCALE, TIME_PERIOD, TIME_COVERAGE)
);

create or replace TABLE POWER_TOOLS_BRAND_SHARE_LANDING (
	METRIC VARCHAR(16777216) NOT NULL COMMENT 'Market metric measured by Numerator',
	PRODUCT_AND_CATEGORY VARCHAR(16777216) NOT NULL COMMENT 'Product parameter for data pull',
	RETAILER VARCHAR(16777216) NOT NULL COMMENT 'Customer group parameter split for data pull',
	BRAND VARCHAR(16777216) NOT NULL COMMENT 'Product brand',
	TIME_SCALE VARCHAR(16777216) NOT NULL COMMENT 'Scale of time frame (e.g. 1 Month Ending, Quarter Ending)',
	TIME_PERIOD VARCHAR(16777216) NOT NULL COMMENT 'Last date to denote end of time_scale',
	TIME_COVERAGE VARCHAR(16777216) NOT NULL COMMENT 'Coverage of time period (e.g. Rolling)',
	VALUE VARCHAR(16777216) COMMENT 'Metric value per the associated parameters',
	LOADDTS VARCHAR(16777216),
	EVENTDTS VARCHAR(16777216),
	REC_SRC VARCHAR(16777216),
	ROW_SQN VARCHAR(16777216),
	primary key (METRIC, PRODUCT_AND_CATEGORY, RETAILER, BRAND, TIME_SCALE, TIME_PERIOD, TIME_COVERAGE)
);


create or replace TABLE QUARTERLY_BRAND_SHARE_LANDING (
	METRIC VARCHAR(16777216) NOT NULL COMMENT 'Market metric measured by Numerator',
	PRODUCT_AND_CATEGORY VARCHAR(16777216) NOT NULL COMMENT 'Product parameter for data pull',
	SBD_QUARTERLY_SCORECARD_STORES VARCHAR(16777216) NOT NULL COMMENT 'Customer group parameter split for data pull',
	BRAND VARCHAR(16777216) NOT NULL COMMENT 'Product brand',
	TIME_SCALE VARCHAR(16777216) NOT NULL COMMENT 'Scale of time frame (e.g. 1 Month Ending, Quarter Ending)',
	TIME_PERIOD VARCHAR(16777216) NOT NULL COMMENT 'Last date to denote end of time_scale',
	TIME_COVERAGE VARCHAR(16777216) NOT NULL COMMENT 'Coverage of time period (e.g. Rolling)',
	VALUE VARCHAR(16777216) COMMENT 'Metric value per the associated parameters',
	LOADDTS VARCHAR(16777216),
	EVENTDTS VARCHAR(16777216),
	REC_SRC VARCHAR(16777216),
	ROW_SQN VARCHAR(16777216),
	primary key (METRIC, PRODUCT_AND_CATEGORY, SBD_QUARTERLY_SCORECARD_STORES, BRAND, TIME_SCALE, TIME_PERIOD, TIME_COVERAGE)
);


create or replace TABLE ROLLING_TWELVE_LANDING (
	METRIC VARCHAR(16777216) NOT NULL COMMENT 'Market metric measured by Numerator',
	PRODUCT_AND_CATEGORY VARCHAR(16777216) NOT NULL COMMENT 'Product parameter for data pull',
	SBD_QUARTERLY_SCORECARD_STORES VARCHAR(16777216) NOT NULL COMMENT 'Customer group parameter split for data pull',
	BRAND VARCHAR(16777216) NOT NULL COMMENT 'Product brand',
	TIME_SCALE VARCHAR(16777216) NOT NULL COMMENT 'Scale of time frame (e.g. 1 Month Ending, Quarter Ending)',
	TIME_PERIOD VARCHAR(16777216) NOT NULL COMMENT 'Last date to denote end of time_scale',
	TIME_COVERAGE VARCHAR(16777216) NOT NULL COMMENT 'Coverage of time period (e.g. Rolling)',
	VALUE VARCHAR(16777216) COMMENT 'Metric value per the associated parameters',
	LOADDTS VARCHAR(16777216),
	EVENTDTS VARCHAR(16777216),
	REC_SRC VARCHAR(16777216),
	ROW_SQN VARCHAR(16777216),
	primary key (METRIC, PRODUCT_AND_CATEGORY, SBD_QUARTERLY_SCORECARD_STORES, BRAND, TIME_SCALE, TIME_PERIOD, TIME_COVERAGE)
);

create or replace TABLE TOTAL_TOOLS_OUTDOORS_NO_LIGHTING_BRAND_SHARE_LANDING (
	METRIC VARCHAR(16777216) NOT NULL COMMENT 'Market metric measured by Numerator',
	PRODUCT_AND_CATEGORY VARCHAR(16777216) NOT NULL COMMENT 'Product parameter for data pull',
	RETAILER VARCHAR(16777216) NOT NULL COMMENT 'Customer group parameter split for data pull',
	BRAND VARCHAR(16777216) NOT NULL COMMENT 'Product brand',
	TIME_SCALE VARCHAR(16777216) NOT NULL COMMENT 'Scale of time frame (e.g. 1 Month Ending, Quarter Ending)',
	TIME_PERIOD VARCHAR(16777216) NOT NULL COMMENT 'Last date to denote end of time_scale',
	TIME_COVERAGE VARCHAR(16777216) NOT NULL COMMENT 'Coverage of time period (e.g. Rolling)',
	VALUE VARCHAR(16777216) COMMENT 'Metric value per the associated parameters',
	LOADDTS VARCHAR(16777216),
	EVENTDTS VARCHAR(16777216),
	REC_SRC VARCHAR(16777216),
	ROW_SQN VARCHAR(16777216),
	primary key (METRIC, PRODUCT_AND_CATEGORY, RETAILER, BRAND, TIME_SCALE, TIME_PERIOD, TIME_COVERAGE)
);